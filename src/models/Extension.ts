export enum ExtensionCallbackType {
	init = 'onInit',
	load = 'onLoad',
	before = 'onBefore',
	start = 'onStart',
	success = 'onSuccess',
	complete = 'onComplete',
	error = 'onError',
}

export enum ExtensionCallbackWrapperType {
	init = 'init',
	load = 'load',
	before = 'before',
	start = 'start',
	success = 'success',
	complete = 'complete',
	error = 'error',
}
